
fn fib(num) {
    if num <= 1 {
        return 1
    }
    return fib(num - 1) + fib(num - 2)
}

//let put = readinput()


for let i = 0; i < 10; i++ {
    println(fib(i))
}

let string = "Hello, World!"

println(string + " " + string.len())